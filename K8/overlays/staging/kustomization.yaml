apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: staging

replicas:
  - name: moja-demo-app
    count: 1

patches:
  - path: patch-env.yaml
    target:
      kind: Deployment
      name: moja-demo-app